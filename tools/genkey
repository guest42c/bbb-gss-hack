#!/bin/sh


#openssl genrsa -passout password -des3 -out server.key 1024

openssl genrsa -out server.key 1024

#openssl req -new -key server.key -out server.csr
openssl req -new -subj "/O=Entropy Wave Inc/OU=autogenerated key" \
  -key server.key -x509 -days 36500 -out server.crt

#cp server.key server.key.org
#openssl rsa -passin password -in server.key.org -out server.key

#openssl x509 -req -days 365 -in server.csr -signkey server.key -out server.crt


